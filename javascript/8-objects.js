/*	OBJECTS

	It's used curly braces <{}> to designate an object literal. An object is organized into key-value pairs.
	Key => Value.

	Syntax:

		let objectName = {
			key: 'value',
		};

*/

	// Example 1 - Creating an object literal:
	let bike = {
		brand: 'Triumph',		// "brand" and "model" are properties.
		model: 'Tiger 900 Rally',
	}